import {Component, Input, Output, EventEmitter, Injectable, Inject, Injector} from '@angular/core';
import {ThreadMapThreadDto} from '../../../data/AllDtos';

/*
    This class will be responsible for transforming the gene set dto into the geneSetD3node
*/
@Injectable()
export class TransformThreadMapThreadDtoService {

    constructor() {

    }

    transform = (threadMapThread : ThreadMapThreadDto) => {

    }

}